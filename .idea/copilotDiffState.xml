<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="$PROJECT_DIR$/js/ai.js">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/js/ai.js" />
              <option name="originalContent" value="window.aiThink = function aiThink(deltaTimeMs) {&#10;    aiTimeAccumulator += deltaTimeMs;&#10;    if (aiTimeAccumulator &lt; 1200) {&#10;        return;&#10;    }&#10;&#10;    aiTimeAccumulator = 0;&#10;&#10;    const units = window.state.units;&#10;    const playerUnits = units.filter(u =&gt; u.owner === 'player');&#10;    const aiUnits = units.filter(u =&gt; u.owner === 'ai');&#10;&#10;    const countByType = arr =&gt;&#10;        arr.reduce((acc, u) =&gt; {&#10;            acc[u.defName] = (acc[u.defName] || 0) + 1;&#10;            return acc;&#10;        }, {});&#10;&#10;    const playerUnitCounts = countByType(playerUnits);&#10;    const aiUnitCounts = countByType(aiUnits);&#10;&#10;    const aiCastleX = window.CONFIG.battlefieldWidth - 40;&#10;    const nearestEnemyX = playerUnits.length ? Math.max(...playerUnits.map(u =&gt; u.x)) : 0;&#10;    const distanceToAiCastle = Math.max(0, aiCastleX - nearestEnemyX);&#10;    const enemyNearCastle = distanceToAiCastle &lt; 200;&#10;&#10;    const aiHasFrontline = aiUnits.some(u =&gt; u.def.type === 'melee' || u.defName === 'hero');&#10;    const aiHeroAlive = aiUnits.some(u =&gt; u.defName === 'hero');&#10;&#10;    function scoreUnit(unitName) {&#10;        const def = window.UNIT_DEFS[unitName];&#10;        const cost = def.cost;&#10;&#10;        // --- Базова бойова ефективність ---&#10;        const attacksPerSec = 1000 / (def.special.attackDelay || 1000);&#10;        const dps = def.dmg * attacksPerSec;&#10;        const effectiveHP = def.hp / (def.speed &lt; 0.7 ? 0.9 : 1); // повільних легше вбити&#10;&#10;        let score = (dps * 0.6 + effectiveHP * 0.4) / cost;&#10;&#10;        // --- Переваги дальності ---&#10;        score += (def.range / 100) * 0.3;&#10;&#10;        // --- Контрпідбір ---&#10;        if (unitName === 'swordsman') score += (playerUnitCounts.mage || 0) * 0.2;&#10;        if (unitName === 'archer') score += (playerUnitCounts.swordsman || 0) * 0.8;&#10;        if (unitName === 'mage') score += (playerUnitCounts.archer || 0) * 0.9;&#10;        if (unitName === 'supreme') score += playerUnits.length &lt;= 2 ? 1.5 : -0.5;&#10;        if (unitName === 'tank') score += playerUnitCounts.supreme * 0.2;&#10;        if (unitName === 'hero') score += enemyNearCastle ? 2.5 : 1;&#10;&#10;        // --- Баланс ролей ---&#10;        if (!aiHasFrontline &amp;&amp; (unitName === 'swordsman' || unitName === 'hero')) score += 1.5;&#10;        if (!aiHasFrontline &amp;&amp; (unitName === 'archer' || unitName === 'mage' || unitName === 'supreme')) score -= 1;&#10;&#10;        // --- Атакуюча ініціатива ---&#10;        if (!enemyNearCastle &amp;&amp; playerUnits.length &lt;= 2) {&#10;            if (unitName === 'swordsman') score -= 3;&#10;            if (unitName === 'archer') score += 0.5;&#10;            if (unitName === 'supreme') score += 1.5;&#10;            if (unitName === 'tank') score += 1.5;&#10;        }&#10;&#10;        if (unitName === 'hero' &amp;&amp; aiHeroAlive) score -= 10;&#10;&#10;        return score;&#10;    }&#10;&#10;    const allUnitTypes = ['swordsman', 'archer', 'mage', 'supreme', 'hero'];&#10;    const scoredUnits = allUnitTypes&#10;        .map(name =&gt; ({ name, score: scoreUnit(name), cost: window.UNIT_DEFS[name].cost }))&#10;        .sort((a, b) =&gt; b.score - a.score);&#10;&#10;    const gold = window.state.aiGold;&#10;&#10;    // --- Рішення накопичувати ---&#10;    const playerGold = window.state.gold;&#10;    const playerNotStockpiling = playerGold &lt; 350;&#10;    const aiSafe = !enemyNearCastle &amp;&amp; playerUnits.length &lt;= 4;&#10;    const noPlayerUnits = playerUnits.length === 0;&#10;&#10;    const bestUnit = scoredUnits[0];&#10;    const shouldSaveForStrongUnit =&#10;        aiSafe &amp;&amp;&#10;        playerNotStockpiling &amp;&amp;&#10;        bestUnit.cost &gt; gold &amp;&amp;&#10;        (bestUnit.name === 'supreme' || bestUnit.name === 'hero');&#10;&#10;    if (shouldSaveForStrongUnit || noPlayerUnits) {&#10;        return; // накопичуємо золото&#10;    }&#10;&#10;    if (playerUnits.length === 1 &amp;&amp; skipCount &lt; 5) {&#10;        aiTimeAccumulator -= 10&#10;        skipCount++&#10;        return;&#10;    }&#10;&#10;    if (playerUnits.length === 1 &amp;&amp; aiUnits.length &gt; 3 &amp;&amp; skipCount &lt; 7) {&#10;        aiTimeAccumulator -= 100&#10;        skipCount++&#10;        return;&#10;    }&#10;&#10;&#10;    if (playerUnits.length === 2 &amp;&amp; aiUnits.length &gt; 5 &amp;&amp; skipCount &lt; 7) {&#10;        aiTimeAccumulator -= 100&#10;        skipCount++&#10;        return;&#10;    }&#10;&#10;    if (playerUnits.length === 2 &amp;&amp; skipCount &lt; 3) {&#10;        aiTimeAccumulator -= 100&#10;        skipCount++&#10;        return;&#10;    }&#10;&#10;    let SUPER_DEFENCE = false&#10;    if (playerUnits.length &gt; 4 &amp;&amp; aiUnits.length * 3 &lt; playerUnits.length &amp;&amp; gold &gt; 1000) {&#10;        SUPER_DEFENCE = true&#10;    }&#10;&#10;    skipCount = 0&#10;&#10;    // --- Спавн ---&#10;    let spawns = 0;&#10;    let remainingGold = gold;&#10;&#10;    for (const candidate of scoredUnits) {&#10;        if (!SUPER_DEFENCE &amp;&amp; spawns &gt;= 2) break;&#10;        if (candidate.cost &lt;= remainingGold &amp;&amp; candidate.score &gt; 0.3) {&#10;            if (candidate.name === 'hero' &amp;&amp; aiHeroAlive) continue;&#10;            window.state.aiGold -= candidate.cost;&#10;            remainingGold -= candidate.cost;&#10;            window.spawnUnit('ai', candidate.name);&#10;            spawns++;&#10;        }&#10;    }&#10;};&#10;&#10;let aiTimeAccumulator = 0;&#10;let skipCount = 0;&#10;" />
              <option name="updatedContent" value="window.aiThink = function aiThink(deltaTimeMs) {&#10;    aiTimeAccumulator += deltaTimeMs;&#10;    if (aiTimeAccumulator &lt; 1200) {&#10;        return;&#10;    }&#10;&#10;    aiTimeAccumulator = 0;&#10;&#10;    const units = window.state.units;&#10;    const playerUnits = units.filter(u =&gt; u.owner === 'player');&#10;    const aiUnits = units.filter(u =&gt; u.owner === 'ai');&#10;&#10;    const countByType = arr =&gt;&#10;        arr.reduce((acc, u) =&gt; {&#10;            acc[u.defName] = (acc[u.defName] || 0) + 1;&#10;            return acc;&#10;        }, {});&#10;&#10;    const playerUnitCounts = countByType(playerUnits);&#10;    const aiUnitCounts = countByType(aiUnits);&#10;&#10;    const aiCastleX = window.CONFIG.battlefieldWidth - 40;&#10;    const nearestEnemyX = playerUnits.length ? Math.max(...playerUnits.map(u =&gt; u.x)) : 0;&#10;    const distanceToAiCastle = Math.max(0, aiCastleX - nearestEnemyX);&#10;    const enemyNearCastle = distanceToAiCastle &lt; 200;&#10;&#10;    const aiHasFrontline = aiUnits.some(u =&gt; u.def.type === 'melee' || u.defName === 'hero');&#10;    const aiHeroAlive = aiUnits.some(u =&gt; u.defName === 'hero');&#10;&#10;    function scoreUnit(unitName) {&#10;        const def = window.UNIT_DEFS[unitName];&#10;        const cost = def.cost;&#10;&#10;        // --- Базова бойова ефективність ---&#10;        const attacksPerSec = 1000 / (def.special.attackDelay || 1000);&#10;        const dps = def.dmg * attacksPerSec;&#10;        const effectiveHP = def.hp / (def.speed &lt; 0.7 ? 0.9 : 1); // повільних легше вбити&#10;&#10;        let score = (dps * 0.6 + effectiveHP * 0.4) / cost;&#10;&#10;        // --- Переваги дальності ---&#10;        score += (def.range / 100) * 0.3;&#10;&#10;        // --- Контрпідбір ---&#10;        if (unitName === 'swordsman') score += (playerUnitCounts.mage || 0) * 0.2;&#10;        if (unitName === 'archer') score += (playerUnitCounts.swordsman || 0) * 0.8;&#10;        if (unitName === 'mage') score += (playerUnitCounts.archer || 0) * 0.9;&#10;        if (unitName === 'supreme') score += playerUnits.length &lt;= 2 ? 1.5 : -0.5;&#10;        if (unitName === 'tank') score += playerUnitCounts.supreme * 0.2;&#10;        if (unitName === 'hero') score += enemyNearCastle ? 2.5 : 1;&#10;        &#10;        // --- Нові юніти ---&#10;        if (unitName === 'assassin') score += (playerUnitCounts.mage || 0) * 0.9 + (playerUnitCounts.archer || 0) * 0.7;&#10;        if (unitName === 'necromancer') score += (playerUnitCounts.tank || 0) * 0.8 + (aiUnitCounts.necromancer || 0) * -0.3;&#10;        if (unitName === 'giant') score += (playerUnitCounts.swordsman || 0) * 0.6 + (enemyNearCastle ? 1.2 : 0);&#10;&#10;        // --- Баланс ролей ---&#10;        if (!aiHasFrontline &amp;&amp; (unitName === 'swordsman' || unitName === 'hero' || unitName === 'giant')) score += 1.5;&#10;        if (!aiHasFrontline &amp;&amp; (unitName === 'archer' || unitName === 'mage' || unitName === 'supreme' || unitName === 'necromancer')) score -= 1;&#10;&#10;        // --- Атакуюча ініціатива ---&#10;        if (!enemyNearCastle &amp;&amp; playerUnits.length &lt;= 2) {&#10;            if (unitName === 'swordsman') score -= 3;&#10;            if (unitName === 'archer') score += 0.5;&#10;            if (unitName === 'supreme') score += 1.5;&#10;            if (unitName === 'tank') score += 1.5;&#10;            if (unitName === 'assassin') score += 2.0;&#10;            if (unitName === 'giant') score -= 1.0;&#10;        }&#10;&#10;        if (unitName === 'hero' &amp;&amp; aiHeroAlive) score -= 10;&#10;&#10;        return score;&#10;    }&#10;&#10;    const allUnitTypes = ['swordsman', 'archer', 'mage', 'supreme', 'hero', 'tank', 'assassin', 'necromancer', 'giant'];&#10;    const scoredUnits = allUnitTypes&#10;        .map(name =&gt; ({ name, score: scoreUnit(name), cost: window.UNIT_DEFS[name].cost }))&#10;        .sort((a, b) =&gt; b.score - a.score);&#10;&#10;    const gold = window.state.aiGold;&#10;&#10;    // --- Рішення накопичувати ---&#10;    const playerGold = window.state.gold;&#10;    const playerNotStockpiling = playerGold &lt; 350;&#10;    const aiSafe = !enemyNearCastle &amp;&amp; playerUnits.length &lt;= 4;&#10;    const noPlayerUnits = playerUnits.length === 0;&#10;&#10;    const bestUnit = scoredUnits[0];&#10;    const shouldSaveForStrongUnit =&#10;        aiSafe &amp;&amp;&#10;        playerNotStockpiling &amp;&amp;&#10;        bestUnit.cost &gt; gold &amp;&amp;&#10;        (bestUnit.name === 'supreme' || bestUnit.name === 'hero' || bestUnit.name === 'giant');&#10;&#10;    if (shouldSaveForStrongUnit || noPlayerUnits) {&#10;        return; // накопичуємо золото&#10;    }&#10;&#10;    if (playerUnits.length === 1 &amp;&amp; skipCount &lt; 5) {&#10;        aiTimeAccumulator -= 10&#10;        skipCount++&#10;        return;&#10;    }&#10;&#10;    if (playerUnits.length === 1 &amp;&amp; aiUnits.length &gt; 3 &amp;&amp; skipCount &lt; 7) {&#10;        aiTimeAccumulator -= 100&#10;        skipCount++&#10;        return;&#10;    }&#10;&#10;&#10;    if (playerUnits.length === 2 &amp;&amp; aiUnits.length &gt; 5 &amp;&amp; skipCount &lt; 7) {&#10;        aiTimeAccumulator -= 100&#10;        skipCount++&#10;        return;&#10;    }&#10;&#10;    if (playerUnits.length === 2 &amp;&amp; skipCount &lt; 3) {&#10;        aiTimeAccumulator -= 100&#10;        skipCount++&#10;        return;&#10;    }&#10;&#10;    let SUPER_DEFENCE = false&#10;    if (playerUnits.length &gt; 4 &amp;&amp; aiUnits.length * 3 &lt; playerUnits.length &amp;&amp; gold &gt; 1000) {&#10;        SUPER_DEFENCE = true&#10;    }&#10;&#10;    skipCount = 0&#10;&#10;    // --- Спавн ---&#10;    let spawns = 0;&#10;    let remainingGold = gold;&#10;&#10;    for (const candidate of scoredUnits) {&#10;        if (!SUPER_DEFENCE &amp;&amp; spawns &gt;= 2) break;&#10;        if (candidate.cost &lt;= remainingGold &amp;&amp; candidate.score &gt; 0.3) {&#10;            if (candidate.name === 'hero' &amp;&amp; aiHeroAlive) continue;&#10;            window.state.aiGold -= candidate.cost;&#10;            remainingGold -= candidate.cost;&#10;            window.spawnUnit('ai', candidate.name);&#10;            spawns++;&#10;        }&#10;    }&#10;};&#10;&#10;let aiTimeAccumulator = 0;&#10;let skipCount = 0;" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/js/config.js">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/js/config.js" />
              <option name="originalContent" value="window.CONFIG = {&#10;    goldRate: 11, // per second (player)&#10;    aiGoldRate: 15, // per second (ai)&#10;    castleHP: 1000,&#10;    battlefieldWidth: 1500,&#10;    unitSize: 48,&#10;    updateInterval: 60 // ms&#10;};&#10;&#10;window.UNIT_DEFS = {&#10;    swordsman: {&#10;        cost: 50,&#10;        hp: 150,&#10;        dmg: 10,&#10;        speed: 0.8,&#10;        range: 50,&#10;        type: 'melee',&#10;        special: { shieldBlock: 0.2 },&#10;        resource: {type: 'stamina', max: 100, regenPerSec: 15, costPerAttack: 16}&#10;    },&#10;    archer: {&#10;        cost: 100,&#10;        hp: 65,&#10;        dmg: 20,&#10;        speed: 1.0,&#10;        range: 180,&#10;        type: 'ranged',&#10;        special: {piercing: 0.5},&#10;        resource: {type: 'stamina', max: 80, regenPerSec: 12, costPerAttack: 15}&#10;    },&#10;    mage: {&#10;        cost: 120,&#10;        hp: 50,&#10;        dmg: 40,&#10;        speed: 2,&#10;        range: 170,&#10;        type: 'ranged',&#10;        special: { chain: 0.5 },&#10;        resource: {type: 'mana', max: 110, regenPerSec: 13, costPerAttack: 20}&#10;    },&#10;    supreme: {&#10;        cost: 200,&#10;        hp: 50,&#10;        dmg: 80,&#10;        speed: 0.35,&#10;        range: 320,&#10;        type: 'siege',&#10;        special: {siege: true, castleBonus: 3.0, line: true, attackDelay: 2400},&#10;        resource: {type: 'mana', max: 120, regenPerSec: 6, costPerAttack: 40}&#10;    },&#10;    hero: {&#10;        cost: 400,&#10;        hp: 300,&#10;        dmg: 40,&#10;        speed: 1.0,&#10;        range: 90,&#10;        type: 'hero',&#10;        special: {unique: true},&#10;        resource: {type: 'stamina', max: 120, regenPerSec: 20, costPerAttack: 25}&#10;    },&#10;    tank: {&#10;        cost: 200,&#10;        hp: 1000,&#10;        dmg: 5,&#10;        speed: 0.5,&#10;        range: 90,&#10;        type: 'melee',&#10;        special: {shieldBlock: 0.8},&#10;        resource: {type: 'stamina', max: 150, regenPerSec: 30, costPerAttack: 35}&#10;    }&#10;};&#10;&#10;&#10;" />
              <option name="updatedContent" value="window.CONFIG = {&#10;    goldRate: 11, // per second (player)&#10;    aiGoldRate: 15, // per second (ai)&#10;    castleHP: 1000,&#10;    battlefieldWidth: 1500,&#10;    unitSize: 48,&#10;    updateInterval: 60 // ms&#10;};&#10;&#10;window.UNIT_DEFS = {&#10;    swordsman: {&#10;        cost: 50,&#10;        hp: 150,&#10;        dmg: 10,&#10;        speed: 0.8,&#10;        range: 50,&#10;        type: 'melee',&#10;        special: { shieldBlock: 0.2 },&#10;        resource: {type: 'stamina', max: 100, regenPerSec: 15, costPerAttack: 16}&#10;    },&#10;    archer: {&#10;        cost: 100,&#10;        hp: 65,&#10;        dmg: 20,&#10;        speed: 1.0,&#10;        range: 180,&#10;        type: 'ranged',&#10;        special: {piercing: 0.5},&#10;        resource: {type: 'stamina', max: 80, regenPerSec: 12, costPerAttack: 15}&#10;    },&#10;    mage: {&#10;        cost: 120,&#10;        hp: 50,&#10;        dmg: 40,&#10;        speed: 2,&#10;        range: 170,&#10;        type: 'ranged',&#10;        special: { chain: 0.5 },&#10;        resource: {type: 'mana', max: 110, regenPerSec: 13, costPerAttack: 20}&#10;    },&#10;    supreme: {&#10;        cost: 200,&#10;        hp: 50,&#10;        dmg: 80,&#10;        speed: 0.35,&#10;        range: 320,&#10;        type: 'siege',&#10;        special: {siege: true, castleBonus: 3.0, line: true, attackDelay: 2400},&#10;        resource: {type: 'mana', max: 120, regenPerSec: 6, costPerAttack: 40}&#10;    },&#10;    hero: {&#10;        cost: 400,&#10;        hp: 300,&#10;        dmg: 40,&#10;        speed: 1.0,&#10;        range: 90,&#10;        type: 'hero',&#10;        special: {unique: true},&#10;        resource: {type: 'stamina', max: 120, regenPerSec: 20, costPerAttack: 25}&#10;    },&#10;    tank: {&#10;        cost: 200,&#10;        hp: 1000,&#10;        dmg: 5,&#10;        speed: 0.5,&#10;        range: 90,&#10;        type: 'melee',&#10;        special: {shieldBlock: 0.8},&#10;        resource: {type: 'stamina', max: 150, regenPerSec: 30, costPerAttack: 35}&#10;    },&#10;    assassin: {&#10;        cost: 150,&#10;        hp: 80,&#10;        dmg: 35,&#10;        speed: 2.2,&#10;        range: 60,&#10;        type: 'melee',&#10;        special: { criticalStrike: 0.3 },&#10;        resource: {type: 'stamina', max: 90, regenPerSec: 18, costPerAttack: 18}&#10;    },&#10;    necromancer: {&#10;        cost: 180,&#10;        hp: 60,&#10;        dmg: 25,&#10;        speed: 0.7,&#10;        range: 200,&#10;        type: 'ranged',&#10;        special: { summonSkeleton: 0.2, attackDelay: 1800 },&#10;        resource: {type: 'mana', max: 150, regenPerSec: 10, costPerAttack: 30}&#10;    },&#10;    giant: {&#10;        cost: 350,&#10;        hp: 800,&#10;        dmg: 60,&#10;        speed: 0.4,&#10;        range: 120,&#10;        type: 'melee',&#10;        special: { areaAttack: 0.5, attackDelay: 2000 },&#10;        resource: {type: 'stamina', max: 180, regenPerSec: 12, costPerAttack: 45}&#10;    }&#10;};" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/js/sound.js">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/js/sound.js" />
              <option name="originalContent" value="// Minimal web audio sound effects without external assets&#10;let audioCtx;&#10;&#10;function getCtx() {&#10;  if (!audioCtx) audioCtx = new (window.AudioContext || window.webkitAudioContext)();&#10;  return audioCtx;&#10;}&#10;&#10;function play(freq = 440, durationMs = 120, type = 'sine', volume = 0.05) {&#10;  // Respect user gesture requirement: if not unlocked, skip playing&#10;  if (!audioCtx &amp;&amp; !(window.AudioContext || window.webkitAudioContext)) return;&#10;  try {&#10;    const ctx = getCtx();&#10;    if (ctx.state === 'suspended') {&#10;      // will be resumed on first click; avoid throwing&#10;      return;&#10;    }&#10;    const osc = ctx.createOscillator();&#10;    const gain = ctx.createGain();&#10;    osc.type = type;&#10;    osc.frequency.value = freq;&#10;    gain.gain.value = volume;&#10;    osc.connect(gain).connect(ctx.destination);&#10;    const now = ctx.currentTime;&#10;    osc.start(now);&#10;    gain.gain.setValueAtTime(volume, now);&#10;    gain.gain.exponentialRampToValueAtTime(0.0001, now + durationMs / 1000);&#10;    osc.stop(now + durationMs / 1000);&#10;  } catch (_) {&#10;    // ignore autoplay-related errors silently&#10;  }&#10;}&#10;&#10;window.sfx = {&#10;  hit() { play(180, 100, 'square', 0.06); },&#10;  crit() { play(280, 140, 'triangle', 0.07); },&#10;  magic() { play(520, 180, 'sine', 0.05); },&#10;  arrow() { play(360, 80, 'sawtooth', 0.04); },&#10;  spawn() { play(240, 120, 'sine', 0.04); },&#10;  victory() { play(660, 400, 'triangle', 0.06); },&#10;  defeat() { play(160, 400, 'square', 0.06); }&#10;};&#10;&#10;&#10;" />
              <option name="updatedContent" value="// Minimal web audio sound effects without external assets&#10;let audioCtx;&#10;let audioContextUnlocked = false;&#10;&#10;function getCtx() {&#10;  if (!audioCtx) audioCtx = new (window.AudioContext || window.webkitAudioContext)();&#10;  return audioCtx;&#10;}&#10;&#10;function unlockAudioContext() {&#10;  if (audioContextUnlocked) return Promise.resolve();&#10;  &#10;  const ctx = getCtx();&#10;  return ctx.resume().then(() =&gt; {&#10;    audioContextUnlocked = true;&#10;  });&#10;}&#10;&#10;// Add this event listener to unlock audio on first user interaction&#10;document.addEventListener('click', function unlockAudio() {&#10;  unlockAudioContext().then(() =&gt; {&#10;    document.removeEventListener('click', unlockAudio);&#10;  });&#10;}, { once: true });&#10;&#10;function play(freq = 440, durationMs = 120, type = 'sine', volume = 0.05) {&#10;  // Respect user gesture requirement: if not unlocked, skip playing&#10;  if (!audioCtx &amp;&amp; !(window.AudioContext || window.webkitAudioContext)) return;&#10;  try {&#10;    const ctx = getCtx();&#10;    if (ctx.state === 'suspended') {&#10;      unlockAudioContext().then(() =&gt; play(freq, durationMs, type, volume));&#10;      return;&#10;    }&#10;    &#10;    const osc = ctx.createOscillator();&#10;    const gain = ctx.createGain();&#10;    osc.type = type;&#10;    osc.frequency.value = freq;&#10;    gain.gain.value = volume;&#10;    osc.connect(gain).connect(ctx.destination);&#10;    const now = ctx.currentTime;&#10;    osc.start(now);&#10;    gain.gain.setValueAtTime(volume, now);&#10;    gain.gain.exponentialRampToValueAtTime(0.0001, now + durationMs / 1000);&#10;    osc.stop(now + durationMs / 1000);&#10;  } catch (err) {&#10;    console.warn('Audio playback error:', err);&#10;  }&#10;}&#10;&#10;window.sfx = {&#10;  hit() { play(180, 100, 'square', 0.06); },&#10;  crit() { play(280, 140, 'triangle', 0.07); },&#10;  magic() { play(520, 180, 'sine', 0.05); },&#10;  arrow() { play(360, 80, 'sawtooth', 0.04); },&#10;  spawn() { play(240, 120, 'sine', 0.04); },&#10;  victory() { play(660, 400, 'triangle', 0.06); },&#10;  defeat() { play(160, 400, 'square', 0.06); }&#10;};" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>